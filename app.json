{
  "name": "srf-frontend-framework",
  "description": "",
  "scripts": {
    "build": "composer install && npm install && gulp"
  },
  "env": {
  },
  "formation": {
  },
  "addons": [

  ],
  "buildpacks": [
    {
      "url": "heroku/php",
      "url": "heroku/nodejs"
    }
  ]
}
