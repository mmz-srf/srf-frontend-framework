<div class="collection {{ styleModifier|default("collection--standard") }}">

    <h3 class="collection__title">
        {% if collection.titleLink is defined %}
        <a href="{{ collection.titleLink|default("#") }}" class="collection__title-link">
            {% endif %}

            {{ collection.title|default("") }}

            {% if collection.titleLink is defined %}
            <span class="collection__title-a11y-help">Landingpage öffnen</span>
            <span class="collection__title-link-icon"></span>
        </a>
        {% endif %}
    </h3>

    {% if collection.lead is defined %}
        <p class="collection__lead">{{collection.lead|default("")}}</p>
    {% endif %}

    <div class="collection__swipeable-area swipeable-area js-swipeable-area">
        <div class="swipeable-area__wrapper js-swipeable-area-wrapper">
            <ul class="collection__teaser-list">
                {% for teaser in collection.teasers %}
                    {% set teaserFormat = 'teaser--size-m' %}
                    <li class="collection__teaser-item swipeable-area__item js-swipeable-area-item">
                        {% include 'molecules-teaser' with {'teaser': teaser, styleModifier: teaserFormat} %}
                    </li>
                {% endfor %}
            </ul>
        </div>
    </div>
</div>

<h3>Demo with hinting</h3>
<div class="collection {{ styleModifier|default("collection--standard") }}">

    <h3 class="collection__title">
        {% if collection.titleLink is defined %}
        <a href="{{ collection.titleLink|default("#") }}" class="collection__title-link">
            {% endif %}

            {{ collection.title|default("") }}

            {% if collection.titleLink is defined %}
            <span class="collection__title-a11y-help">Landingpage öffnen</span>
            <span class="collection__title-link-icon"></span>
        </a>
        {% endif %}
    </h3>

    {% if collection.lead is defined %}
        <p class="collection__lead">{{collection.lead|default("")}}</p>
    {% endif %}

    <div class="collection__swipeable-area swipeable-area js-swipeable-area" data-swipeable-hinting="true">
        <div class="swipeable-area__wrapper js-swipeable-area-wrapper">
            <ul class="collection__teaser-list">
                {% for teaser in collection.teasers %}
                    {% set teaserFormat = 'teaser--size-m' %}
                    <li class="collection__teaser-item swipeable-area__item js-swipeable-area-item">
                        {% include 'molecules-teaser' with {'teaser': teaser, styleModifier: teaserFormat} %}
                    </li>
                {% endfor %}
            </ul>
        </div>
    </div>
</div>

<h3>Demo with visible-item-class</h3>
<div class="collection {{ styleModifier|default("collection--standard") }}">

    <h3 class="collection__title">
        {% if collection.titleLink is defined %}
        <a href="{{ collection.titleLink|default("#") }}" class="collection__title-link">
            {% endif %}

            {{ collection.title|default("") }}

            {% if collection.titleLink is defined %}
            <span class="collection__title-a11y-help">Landingpage öffnen</span>
            <span class="collection__title-link-icon"></span>
        </a>
        {% endif %}
    </h3>

    {% if collection.lead is defined %}
        <p class="collection__lead">{{collection.lead|default("")}}</p>
    {% endif %}

    <div class="collection__swipeable-area swipeable-area js-swipeable-area" data-mark-hidden-items="swipeable-area-item--hidden">
        <div class="swipeable-area__wrapper js-swipeable-area-wrapper">
            <ul class="collection__teaser-list">
                {% for teaser in collection.teasers %}
                    {% set teaserFormat = 'teaser--size-m' %}
                    <li class="collection__teaser-item swipeable-area__item js-swipeable-area-item">
                        {% include 'molecules-teaser' with {'teaser': teaser, styleModifier: teaserFormat} %}
                    </li>
                {% endfor %}
            </ul>
        </div>
    </div>
</div>

<h3>Demo with many more items and hinting and marking hidden items</h3>
<div class="collection {{ styleModifier|default("collection--standard") }}">

    <h3 class="collection__title">
        {% if collection.titleLink is defined %}
        <a href="{{ collection.titleLink|default("#") }}" class="collection__title-link">
            {% endif %}

            {{ collection.title|default("") }}

            {% if collection.titleLink is defined %}
            <span class="collection__title-a11y-help">Landingpage öffnen</span>
            <span class="collection__title-link-icon"></span>
        </a>
        {% endif %}
    </h3>

    {% if collection.lead is defined %}
        <p class="collection__lead">{{collection.lead|default("")}}</p>
    {% endif %}

    <div class="collection__swipeable-area swipeable-area js-swipeable-area" data-swipeable-hinting="true" data-mark-hidden-items="swipeable-area-item--hidden">
        <div class="swipeable-area__wrapper js-swipeable-area-wrapper">
            <ul class="collection__teaser-list">
                {% for teaser in collection.teasers %}
                    {% set teaserFormat = 'teaser--size-m' %}
                    <li class="collection__teaser-item swipeable-area__item js-swipeable-area-item">
                        {% include 'molecules-teaser' with {'teaser': teaser, styleModifier: teaserFormat} %}
                    </li>
                {% endfor %}
                {% for teaser in collection.teasers %}
                    {% set teaserFormat = 'teaser--size-m' %}
                    <li class="collection__teaser-item swipeable-area__item js-swipeable-area-item">
                        {% include 'molecules-teaser' with {'teaser': teaser, styleModifier: teaserFormat} %}
                    </li>
                {% endfor %}
                {% for teaser in collection.teasers %}
                    {% set teaserFormat = 'teaser--size-m' %}
                    <li class="collection__teaser-item swipeable-area__item js-swipeable-area-item">
                        {% include 'molecules-teaser' with {'teaser': teaser, styleModifier: teaserFormat} %}
                    </li>
                {% endfor %}
            </ul>
        </div>
    </div>
</div>
