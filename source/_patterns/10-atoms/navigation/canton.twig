{% set canton = canton is defined and canton is not empty ? canton : {'url': '#', 'name': 'Zürich', 'shortcut': 'zh'} %}

<div class="canton-navpoint canton-navpoint--{{ canton.shortcut|lower() }}">
    <a class="canton-navpoint__link" href="{{ canton.url }}" title="{{ canton.name }}">
        {{ canton.shortcut|upper() }}
    </a>
</div>
