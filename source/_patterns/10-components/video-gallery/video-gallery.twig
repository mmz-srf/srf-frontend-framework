<div class="video-gallery-container {{ styleModifierGallery }}">
    <button class="video-gallery-container__player-close" value="{{ 'Schliessen'|trans }}"></button>
    <div class="video-gallery-container__player"><!-- here we expect a player --></div>
    <div class="video-gallery carousel video_carousel__js" id="{{ video_gallery.ID }}">
        {% for video in videos %}
            <div class="carousel__item">
                {% include 'components-video' with { 'styleModifier' : styleModifier } %}
            </div>
        {% endfor %}
    </div>
</div>
